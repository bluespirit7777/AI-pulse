.claude/
node_modules/
.DS_Store

# Design/media source material, kept locally but never deployed. The hero loop
# that ships lives in assets/ (cropped + compressed from "Wave gif/"); these
# originals are ~8MB of 720p mp4 and a ~8MB self-unpacking design bundle, none
# of which GitHub Pages needs to serve.
Wave gif/
AI Market Pulse - Landing (standalone_design).html
